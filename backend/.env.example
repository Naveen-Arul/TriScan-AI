# MongoDB Connection URI
# For local MongoDB: mongodb://localhost:27017/triscan-ai
# For MongoDB Atlas: mongodb+srv://<username>:<password>@cluster.mongodb.net/triscan-ai
MONGO_URI=mongodb://localhost:27017/triscan-ai

# JWT Secret Key
# Generate a strong random secret (e.g., using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Server Port
PORT=5000

# Node Environment
NODE_ENV=development

# Groq API Key for LLM text cleanup
# Get your free API key from: https://console.groq.com
GROQ_API_KEY=your-groq-api-key-here
